name: Backend Batch Server CI

on:
  workflow_dispatch:

  push:
    branches: [ "develop", "feature/**" ]
#    paths:
#      - 'batch-server/**'
  pull_request:
    branches: [ "main", "develop" ]
    paths:
      - 'batch-server/**'

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      statuses: write
      checks: write
      pull-requests: write

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'corretto'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@ad2b38190b15e4d6bdf0c97fb4fca8412226d287

      - name: Grant Execute Permission for Gradlew
        run: chmod +x ./gradlew

      - name: Test and Build with Gradle
        run: ./gradlew :batch-server:clean build